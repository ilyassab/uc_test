import contentStyles from '../UserCardContent/styles';

export default `
  .user-card__container {
    width: 350px;
    height: 150px;
    border-radius: 4px;
    background-color: rgba(0, 0, 0, 0.05);
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.25);
    display: flex;
    padding: 0 10px;
    align-items: center;
  }
  
  ${contentStyles}
`;
